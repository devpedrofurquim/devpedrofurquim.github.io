import { Box } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const StorySection = styled(Box)`
    padding-left: 3.4em;
    text-indet: -3.4em;
`

export const StoryYear = styled.span `
    font-weight: bold;
    margin-right: 1em;
`